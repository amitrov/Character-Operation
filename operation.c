#include <stdio.h>

int main() {
    int input;

    
    printf("Enter an integer: ");
    scanf("%d", &input);

    
    printf("The corresponding character is: %c\n", (char)input);

    return 0;
}
